# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseConfig, BaseModel, Field, validator

from . import types, enums, errors, fields, bases
from ._types import FuncType
from .builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class RECPIES(bases.BaseRECPIES):
    """Represents a RECPIES record"""

    id: _int
    name: _str
    slug: Optional[_str]
    video_url: Optional[_str]
    thumbnail_url: Optional[_str]
    USER_likes_RECPIES: Optional[List['models.USER_likes_RECPIES']]

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.RECPIESKeys']] = None,
        exclude: Optional[Iterable['types.RECPIESKeys']] = None,
        required: Optional[Iterable['types.RECPIESKeys']] = None,
        optional: Optional[Iterable['types.RECPIESKeys']] = None,
        relations: Optional[Mapping['types.RECPIESRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.RECPIESKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _RECPIES_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _RECPIES_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _RECPIES_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _RECPIES_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _RECPIES_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _RECPIES_relational_fields:
                        raise errors.UnknownRelationalFieldError('RECPIES', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid RECPIES / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'RECPIES',
            }
        )
        _created_partial_types.add(name)


class USERS(bases.BaseUSERS):
    """Represents a USERS record"""

    id: _int
    name: Optional[_str]
    USER_likes_RECPIES: Optional[List['models.USER_likes_RECPIES']]

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.USERSKeys']] = None,
        exclude: Optional[Iterable['types.USERSKeys']] = None,
        required: Optional[Iterable['types.USERSKeys']] = None,
        optional: Optional[Iterable['types.USERSKeys']] = None,
        relations: Optional[Mapping['types.USERSRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.USERSKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _USERS_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _USERS_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _USERS_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _USERS_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _USERS_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _USERS_relational_fields:
                        raise errors.UnknownRelationalFieldError('USERS', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid USERS / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'USERS',
            }
        )
        _created_partial_types.add(name)


class USER_likes_RECPIES(bases.BaseUSER_likes_RECPIES):
    """Represents a USER_likes_RECPIES record"""

    user: Optional['models.USERS']
    USER_id: _int
    recipe: Optional['models.RECPIES']
    RECIPE_id: _int

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.USER_likes_RECPIESKeys']] = None,
        exclude: Optional[Iterable['types.USER_likes_RECPIESKeys']] = None,
        required: Optional[Iterable['types.USER_likes_RECPIESKeys']] = None,
        optional: Optional[Iterable['types.USER_likes_RECPIESKeys']] = None,
        relations: Optional[Mapping['types.USER_likes_RECPIESRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.USER_likes_RECPIESKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _USER_likes_RECPIES_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _USER_likes_RECPIES_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _USER_likes_RECPIES_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _USER_likes_RECPIES_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _USER_likes_RECPIES_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _USER_likes_RECPIES_relational_fields:
                        raise errors.UnknownRelationalFieldError('USER_likes_RECPIES', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid USER_likes_RECPIES / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'USER_likes_RECPIES',
            }
        )
        _created_partial_types.add(name)



_RECPIES_relational_fields: Set[str] = {
        'USER_likes_RECPIES',
    }
_RECPIES_fields: Dict['types.RECPIESKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slug', {
            'name': 'slug',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('video_url', {
            'name': 'video_url',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('thumbnail_url', {
            'name': 'thumbnail_url',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('USER_likes_RECPIES', {
            'name': 'USER_likes_RECPIES',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.USER_likes_RECPIES\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_USERS_relational_fields: Set[str] = {
        'USER_likes_RECPIES',
    }
_USERS_fields: Dict['types.USERSKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('USER_likes_RECPIES', {
            'name': 'USER_likes_RECPIES',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.USER_likes_RECPIES\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_USER_likes_RECPIES_relational_fields: Set[str] = {
        'user',
        'recipe',
    }
_USER_likes_RECPIES_fields: Dict['types.USER_likes_RECPIESKeys', PartialModelField] = OrderedDict(
    [
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.USERS',
            'is_relational': True,
            'documentation': None,
        }),
        ('USER_id', {
            'name': 'USER_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('recipe', {
            'name': 'recipe',
            'is_list': False,
            'optional': True,
            'type': 'models.RECPIES',
            'is_relational': True,
            'documentation': None,
        }),
        ('RECIPE_id', {
            'name': 'RECIPE_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
RECPIES.update_forward_refs()
USERS.update_forward_refs()
USER_likes_RECPIES.update_forward_refs()
